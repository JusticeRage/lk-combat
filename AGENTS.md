
- If a new primitive is created to support spells, always update summarizeSpellEffect() so that the spell can be described properly.
- Document every hotkey in the hotkey dialog whenever one is added to the app.
